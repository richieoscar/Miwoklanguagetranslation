<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.richieoscar.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Miwok">
        <activity android:name=".ui.SplashActivity" android:theme="@style/myTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.PhraseActivity"
            android:label="@string/phrases"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.ColorActivity"
            android:label="@string/colors"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.FamilyActivity"
            android:label="@string/family_members"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.NumberActivity"
            android:label="@string/numbers"
            android:parentActivityName=".ui.MainActivity" />
        <activity android:name=".ui.MainActivity">
        </activity>
    </application>

</manifest>